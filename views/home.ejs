<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Select Subject</title>
  <link rel="stylesheet" href="/css/styles.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="icon" type="image/png" href="/favicon.png">
</head>

<body class="bg-slate-100 min-h-screen flex items-center justify-center p-6">

  <div class="w-full max-w-4xl">
    <h1 class="text-2xl sm:text-3xl font-bold text-slate-900 mb-6 text-center">
      Select a Subject
    </h1>

    <!-- CARD GRID -->
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 sm:gap-6">

      <!-- PhysSci Card -->
      <a href="/physci"
         class="p-5 sm:p-6 bg-white shadow-md hover:shadow-xl transition-all duration-300 hover:-translate-y-1 rounded-2xl border border-slate-200 flex items-start gap-4">
        
        <div class="w-12 h-12 sm:w-14 sm:h-14 rounded-xl bg-blue-500/10 flex items-center justify-center">
          <span class="text-blue-500 text-2xl">🔬</span>
        </div>

        <div class="flex-1">
          <h2 class="text-lg sm:text-xl font-semibold text-slate-900 mb-1">
            Physical Science
          </h2>
          <p class="text-sm text-slate-500 leading-snug">
            Modules, activities, and worksheets for Physical Science.
          </p>
        </div>
      </a>

      <!-- MIL Card -->
      <a href="/mil"
         class="p-5 sm:p-6 bg-white shadow-md hover:shadow-xl transition-all duration-300 hover:-translate-y-1 rounded-2xl border border-slate-200 flex items-start gap-4">
        
        <div class="w-12 h-12 sm:w-14 sm:h-14 rounded-xl bg-purple-500/10 flex items-center justify-center">
          <span class="text-purple-500 text-2xl">💡</span>
        </div>

        <div class="flex-1">
          <h2 class="text-lg sm:text-xl font-semibold text-slate-900 mb-1">
            Media & Information Literacy
          </h2>
          <p class="text-sm text-slate-500 leading-snug">
            Learn about media, information literacy, and digital citizenship.
          </p>
        </div>
      </a>

      <!-- Add More Subjects Here -->
      <% if (typeof extraSubjects !== "undefined") { %>
        <% extraSubjects.forEach(subj => { %>
          <a href="/<%= subj.link %>"
             class="p-5 sm:p-6 bg-white shadow-md hover:shadow-xl transition-all duration-300 hover:-translate-y-1 rounded-2xl border border-slate-200 flex items-start gap-4">
            
            <div class="w-12 h-12 sm:w-14 sm:h-14 rounded-xl bg-green-500/10 flex items-center justify-center">
              <span class="text-green-500 text-2xl"><%= subj.icon %></span>
            </div>

            <div class="flex-1">
              <h2 class="text-lg sm:text-xl font-semibold text-slate-900 mb-1">
                <%= subj.title %>
              </h2>
              <p class="text-sm text-slate-500 leading-snug">
                <%= subj.description %>
              </p>
            </div>
          </a>
        <% }) %>
      <% } %>

    </div>

  </div>

</body>
</html>
